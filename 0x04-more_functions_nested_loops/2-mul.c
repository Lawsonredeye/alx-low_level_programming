#include "main.h"

/**
 * mul - multiplies two integers
 * @a: first variable to be multiplied
 * @b: second varable to be multiplied
 * Return: return result
 */
int mul(int a, int b)
{
	int multi_int = a * b;

	return (multi_int);
}
