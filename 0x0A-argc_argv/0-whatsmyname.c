#include <stdio.h>

/**
 * main - entry point for code execution
 * @argc: int var to main
 * @argv: pointer var to main
 * Return: 0 always
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	printf("%d\n", argc);
	return (0);
}
