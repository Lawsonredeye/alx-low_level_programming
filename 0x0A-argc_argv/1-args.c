#include <stdio.h>

/**
 * main - entry point for code execution
 * @argv: int var to main
 * Return: 0 always
 */
int main(int argc, char *argv[])
{
	(void)argv;

	printf("%d\n", argc);
	return (0);
}
