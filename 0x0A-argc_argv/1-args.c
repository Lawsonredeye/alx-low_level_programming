#include <stdio.h>

/**
 * main - entry point for code execution
 * @argc: int var to main
 * Return: 0 always
 */
int main(int argc, char *argv[])
{
	(void)argv;

	printf("%d", argc);
	return (0);
}
